## 简介

rk3399 有 usb3.0 5Gbps otg，这点对于可以连接电脑的设备很好，传输数据的速度快  
用的是 firefly-rk3399 板卡

### 启动流程

rk3399 芯片内有 192KByte sram 和 32KByte rom  
启动时首先运行芯片内部预先存储的程序，该程序也称为 bootrom、maskrom，该程序从数个外部存储设备中找“ID Block”信息（查找顺序是 SPI Nor > SPI Nand > eMMC > SD/MMC/TF），找到了就认该设备为启动盘  
bootrom 加载 tpl 到芯片内部的 sram 并执行，tpl 会初始化 dram，结束后回到 bootrom，bootrom 再加载 spl 到 dram 并执行，此时 bootrom 就结束了。spl 会将 u-boot.itb 加载到 dram 中并执行

tpl 因为要初始化 dram 所以包含 dram 的驱动，这样 spl 就不需要驱动了。tpl 和 spl 都来自 uboot

### 启动盘里的数据分布

距离起点的位置：

```txt
sector(512Byte)   : byte                             : file
0x40     d64      : 0x8000     d3_2768      32 KiB : idbloader.img
0x4000   d1_6384  : 0x80_0000  d838_8608    8  MiB : u-boot.itb
0x8000   d3_2768  : 0x100_0000 d1677_7216   16 MiB : boot.img
0x40000  d26_2144 : 0x800_0000 d1_3421_7728 128MiB : rootfs.img
```

linux 里制作烧录镜像要先写好所有的分区表信息，再烧录文件。因为 linux 的 fdisk 会清空镜像的前 2048 个扇区，也就是 1MiB  
还需要 GPT分区表，但只有在 u-boot.itb（不包括u-boot.itb）后面的需要添加分区信息。分区信息只需位置和大小，分区里的文件系统由分立的镜像文件决定，比如 rootfs 分区的格式由 rootfs.img 本身的文件系统类型绝定  
镜像释放后表现为前 16MiB 是空闲，boot.img 里的文件在一个 fat16 分区（fat32 所在的空间不能低于 32MiB），rootfs.img 里的文件在一个 ext4 分区  
如果分区需要文件系统，比如 boot.img，就需要在制作 boot.img 时就将 boot.img 格式化为 fat16，并把对应的文件复制进去。之后就准备个大的 .img 文件，并写好分区表信息，再把各个镜像文件按照要求的位置写到大镜像文件里。rootfs.img 起点位置可能不固定

- idbloader.img  
    主要包含 ID Block 包头、u-boot-tpl.bin、u-boot-spl.bin  
    这是 rk 芯片的私有格式  

- u-boot.itb  
    主要包含 u-boot-nodtd.bin、bl31.elf、u-boot.dtd，而旧式的 .img 不包含 bl31.elf，也就是 trust.img  
    bl31.elf 包含 TF-A 的 dram、sram、m0 的 .bin 文件。rk3399 内部 有两个 Cortex-M0，PMU M0 被 TF-A 使用，Perilp M0 给用户使用

- boot.img  
    主要包含 内核镜像、内核用的设备树

- rootfs.img  
    主要包含 用户使用的程序和文件

## 开发环境

只需要启动到内核即可，根文件系统在电脑上制作并在 qemu 里验证，之后再烧录到 tf卡 里

在 docker 里的 ubuntu 里进行开发

1. 创建并运行容器

    ```
    docker run -itd --privileged --name ubuntu_20_04 ubuntu:20.04
    ```

    -itd：i：可交互。t：交互方式是终端。d：这次不进入容器。i 和 t 必须设置，不然容器无法运行  
    --privileged：可以有 root 权限。该容器默认是 root 用户，但是不加这个依然没有 root 权限  
    ubuntu_20_04：容器名  
    ubuntu:20.04：镜像名和版本
    
    没有镜像会自动下载

2. 进入容器

    ```
    docker exec -it ubuntu_20_04 bash
    ```

    bash：进入容器后执行 bash，一个终端程序。这样才能正常使用终端

    也可以在 vscode 安装 docker 扩展，右击容器选择 Attach Shell 就能让容器 shell 出现在 vscode 内嵌的终端上

3. 更新软件源

    ```
    apt update
    ```

    本地没有软件源是下载不了软件的

4. 安装软件

    ```
    apt install vim ca-certificates
    ```

    vim：文本编辑器。镜像没有文本编辑器  
    ca-certificates：CA 证书，证明链接是安全的，这个带有清华源的证书。清华源需要这个  

5. 换源

    1. 打开文件并删除其全部内容

        ```
        vim /etc/apt/sources.list
        ```

        全部删除：手动输入 ggdG

    2. 复制此处，然后在终端里右击就粘贴了

        ```txt
        deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal main restricted universe multiverse
        deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-updates main restricted universe multiverse
        deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-backports main restricted universe multiverse
        deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-security main restricted universe multiverse
        ```

        focal 是 ubuntu 20 的代号，不同版本的系统就更改这个代号，其他的保持原样

        保存退出：按 esc 到命令模式，再 shift + :，再输入 wq，然后回车

6. 更新软件源和更新本地软件

    ```
    apt update
    apt upgrade
    ```

7. 安装软件

    ```
    apt install sudo
    ```

8. 设置 root 用户密码

    ```
    passwd root
    ```

    然后设置密码

    默认是 root 用户，

9. 添加普通用户

    ```
    adduser xiaguangbo
    ```

    xiaguangbo：用户名  

    然后设置密码。剩下的参数使用默认值

10. 让用户可以获得 root 权限

    ```
    vim /etc/sudoers
    ```

    在 20 行左右找到 root ALL=(ALL) ALL，在下面添加 xiaguangbo ALL=(ALL) ALL，然后强制写入并退出（在命令后面加感叹号），因为该文件是只读权限

11. 切换到普通用户

    ```
    su xiaguangbo
    ```

12. 创建工作目录

    ```
    mkdir ~/ubox
    ```

### 用 yocto 制作

1. 安装软件

    ```
    sudo apt install tar gawk wget git diffstat unzip texinfo gcc build-essential chrpath socat cpio python3 python3-pip python3-pexpect xz-utils debianutils iputils-ping python3-git python3-jinja2 libegl1-mesa libsdl1.2-dev pylint3 xterm python3-subunit mesa-common-dev zstd liblz4-tool locales-all iproute2
    ```

    过程中会提示选时区，选择 Asis/Shanghai

    git 的设置：

    ```
    git config --global user.email "1617529183@qq.com"
    git config --global user.name "xiaguangbo"
    ```

2. 创建工作目录

    ```
    mkdir ~/ubox/yocto
    ```

#### 简单使用 yocto

<https://docs.yoctoproject.org/brief-yoctoprojectqs/index.html>

1. 下载

    ```
    cd ~/ubox/yocto
    git clone https://github.com/yoctoproject/poky.git
    ```

2. 编译

    ```
    cd poky
    source oe-init-build-env
    bitbake core-image-minimal
    ```

    登录或切换了用户就需要重新执行 source oe-init-build-env

3. 运行

    ```
    runqemu qemux86-64 nographic
    ```

    由于没有图形界面所以要加 nographic

    登录 root 用户：

    ```
    root
    ```

    退出 qemu：ctrl + a，x。在这之前要退出用户回到登录处：exit


##### 添加已有的层

```
cd ~/ubox/yocto/poky
git clone https://github.com/kraj/meta-altera.git
bitbake-layers add-layer meta-altera
```

##### 创建层并添加层

```
cd ~/ubox/yocto/poky
bitbake-layers create-layer meta-rockchip
bitbake-layers add-layer meta-rockchip
```