bcd 编辑器

## 使用

- 添加 vhdx 启动项

    选择 BCD 编辑/当前系统 BCD/智能编辑模式，会进入另一个界面  
    选择左半边的 添加/新建 VHD 启动项  
    选择右半边的 VHD(X)，“启动硬盘”选择 vhdx 文件所在的硬盘；“启动分区”选择 vhdx 文件所在的分区；“设备文件”填写 vhdx 文件的路径，比如 \windows.vhdx，不加盘符是因为“启动分区”那已经有盘符了  
    选择 保存当前系统设置。保存全局设置 是用于 全局设置 部分的