## 简介

rk3399 芯片内有 192KiB sram 和 32KiB rom  
启动时首先运行芯片内部预先存储的程序，该程序也称为 bootrom、maskrom，该程序从数个外部存储设备中找“ID Block”信息（查找顺序是 SPI Nor > SPI Nand > eMMC > SD/MMC/TF），找到了就认该设备为启动盘

启动盘里的数据距离起点的位置：
```txt
sector(512Byte)   : byte                           : file
0x40     d64      : 0x8000     d3_2768      32 KiB : idbloader.img
0x4000   d1_6384  : 0x80_0000  d838_8608    8  MiB : u-boot.itb
0x8000   d3_2768  : 0x100_0000 d1677_7216   16 MiB : boot.img
0x40000  d26_2144 : 0x800_0000 d1_3421_7728 128MiB : rootfs.img（也可以紧挨在 boot.img 后面）
```

linux 里制作烧录镜像要先写好所有的分区表信息，再烧录文件。因为 linux 的 fdisk 会清空镜像的前 2048 个扇区，也就是 1MiB  
还需要 GPT分区表，但只有在 u-boot.itb（不包括u-boot.itb）后面的需要添加分区信息。分区信息只需位置和大小，分区里的文件系统由分立的镜像文件决定，比如 rootfs 分区的格式由 rootfs.img 本身的文件系统类型绝定  
镜像释放后表现为前 16MiB 是空闲，boot.img 里的文件在一个 fat16 分区（fat32 所在的空间不能低于 32MiB），rootfs.img 里的文件在一个 ext4 分区  
如果分区需要文件系统，比如 boot.img，就需要在制作 boot.img 时就将 boot.img 格式化为 fat16，并把对应的文件复制进去。之后就准备个大的 .img 文件，并写好分区表信息，再把各个镜像文件按照要求的位置写到大镜像文件里

- idbloader.img：包含 idblock.img、u-boot-tpl.bin、u-boot-spl.bin
- u-boot.itb：包含 u-boot-nodtd.bin、bl31.elf、u-boot.dtd
- boot.img：包含内核镜像、内核用的设备树
- rootfs.img：包含用户使用的程序和文件

## 开发环境

### 在 docker 里的 linux 里进行开发
  
windows 安装 docker desktop 时会提示要安装一个适用于 wsl 的包，手动安装后继续安装 docker desktop  
docker 命令需要软件启动后才能执行

1. 创建容器

    ```
    docker create -itd --privileged --name archlinux_container archlinux
    ```

    -itd：i：可交互。t：交互方式是终端。d：不直接进入容器。i 和 t 必须设置，不然容器无法运行  
    --privileged：可以有 root 权限。该容器默认是 root 用户，但是不加这个依然没有 root 权限  

2. 运行容器

    ```
    docker start archlinux_container
    ```

    停止容器：docker stop archlinux_container

3. 进入容器

    ```
    docker exec -it archlinux_container bash
    ```

    bash：进入容器后执行 bash，一个终端程序。这样才能正常使用终端

    退出 bash：exit

4. 添加软件仓库

    在 /etc/pacman.d/mirrorlist 第一行添加：

    ```
    Server = https://mirrors.tuna.tsinghua.edu.cn/archlinux/$repo/os/$arch
    ```

5. 更新本地软件仓库信息并更新所有软件

    ```
    pacman -Sy
    pacman -Syu
    ```

    pacman -S 软件名: 安装软件  
    pacman -Ss 关键字: 在软件仓库中搜索含关键字的软件包  
    pacman -Qs 关键字: 搜索已安装的软件包  
    pacman -Ru 软件名: 删除软件，同时删除不需要的依赖  

    搜索命令以获取包名：https://man.archlinux.org/man/rpcgen.1

6. 安装软件

    ```
    pacman -S vim sudo
    ```

7. 添加普通用户并修改密码

    ```
    useradd -m xiaguangbo
    passwd xiaguangbo
    ```

    用户组和用户名都是这 xiaguangbo

8. 让用户可以获得 root 权限

    ```
    vim /etc/sudoers
    ```

    找到 root ALL=(ALL:ALL) ALL，在下面添加：
    
    ```
    xiaguangbo ALL=(ALL:ALL) ALL
    ```
    
    该文件需强制写入

9. 切换到普通用户

    ```
    su xiaguangbo
    ```

### 用 buildroot 制作

下载的文件在 dl 下，有的是源码有的是压缩包，比如 arm-trusted-firmware、u-boot  
用的文件在 output/build 下，包括下载的源码和解压的，比如 arm-trusted-firmware、u-boot  
输出的镜像在 output/images 下，有完整镜像和分立的镜像，比如 idbloader.img、u-boot.itb、rootfs.ext4

1. 安装软件

    ```
    apt install -y sed make binutils build-essential gcc g++ bash gzip bzip2 tar cpio unzip rsync file bc libssl-dev wget git
    ```

    ```
    git config --global user.email "1617529183@qq.com"
    git config --global user.name "xiaguangbo"
    ```

2. 下载 buildroot

    ```
    mkdir -p ~/project/rockchip_buildroot
    cd ~/project/rockchip_buildroot
    git clone --depth 1 https://github.com/buildroot/buildroot.git
    ```

#### 使用本地源码

```
mkdir source
cd source

git clone --depth 1 https://github.com/ARM-software/arm-trusted-firmware.git
git clone --depth 1 https://github.com/u-boot/u-boot.git
git clone --depth 1 https://github.com/torvalds/linux.git
```

```
cd arm-trusted-firmware
rm -rf .git
git init
git add .
git commit -m "1"

cd ../u-boot
rm -rf .git
git init
git add .
git commit -m "1"

cd ../linux
rm -rf .git
git init
git add .
git commit -m "1"
```

删除这些目录里的 .git 目录，重新使用 git 初始化，不然用不了。因为 buildroot 要求是一个完整的仓库，但我不想下载整个仓库，我只留下了最新的版本，所以需要将其弄成完整的仓库

#### 增加自己的配置

增加这些文件：  
board/firefly/firefly-rk3399/extlinux.conf  
board/firefly/firefly-rk3399/genimage.cfg  
board/firefly/firefly-rk3399/post-build.sh  
configs/firefly-rk3399_defconfig

##### 创建 firefly-rk3399 目录

```
cd ~/project/rockchip_buildroot/buildroot
mkdir board/firefly/firefly-rk3399
```

##### 添加 extlinux.conf

1. 创建

    ```
    vim board/firefly/firefly-rk3399/extlinux.conf
    ```

2. 内容

    ```txt
    label firefly-rk3399 linux
        kernel /Image
        devicetree /rk3399-firefly.dtb
        append earlycon=uart8250,mmio32,0xff1a0000 root=/dev/mmcblk1p2 rootwait
    ```

    kernel 和 devicetree 指的是编译完成后生成的 output/images 下的文件

##### 添加 genimage.cfg

1. 创建

    ```
    vim board/firefly/firefly-rk3399/genimage.cfg
    ```

2. 内容

    ```txt
    image boot.vfat {
        vfat {
            files = {
                "Image",
                "rk3399-firefly.dtb",
                "extlinux"
            }
        }

        size = 64M
    }

    image sdcard.img {
        hdimage {
            partition-table-type = "gpt"
        }

        partition loader1 {
            in-partition-table = "no"
            image = "idbloader.img"
            offset = 32K
        }

        partition loader2 {
            in-partition-table = "no"
            image = "u-boot.itb"
            offset = 8M
        }

        partition boot {
            image = "boot.vfat"
            offset = 16M
        }

        partition rootfs {
            image = "rootfs.ext4"
        }
    }
    ```

    boot.vfat/vfat/files 指的是指编译完成后生成的 output/images 下的文件

##### 添加 post-build.sh

1. 创建

    ```
    vim board/firefly/firefly-rk3399/post-build.sh
    ```

2. 内容

    ```sh
    #!/bin/sh

    BOARD_DIR="$(dirname $0)"

    install -m 0644 -D $BOARD_DIR/extlinux.conf $BINARIES_DIR/extlinux/extlinux.conf
    ```

    $BOARD_DIR：这应该是和 configs/firefly_rk3399_defconfig 里的 BR2_TARGET_GENERIC_HOSTNAME 有关  
    $BINARIES_DIR：output/images

3. 添加权限

    ```
    chmod 777 board/firefly/firefly-rk3399/post-build.sh
    ```

##### 添加 firefly_rk3399_defconfig

1. 创建

    ```
    vim configs/firefly_rk3399_defconfig
    ```

2. 内容

    ```txt
    # Architecture
    BR2_aarch64=y
    BR2_cortex_a72_a53=y

    # Firmware
    BR2_TARGET_ARM_TRUSTED_FIRMWARE=y
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_CUSTOM_GIT=y
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_CUSTOM_REPO_URL="/home/xiaguangbo/project/rockchip_buildroot/source/arm-trusted-firmware"
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_CUSTOM_REPO_VERSION="origin/master"
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_PLATFORM="rk3399"
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_BL31=y
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_NEEDS_ARM32_TOOLCHAIN=y
    BR2_TARGET_ARM_TRUSTED_FIRMWARE_IMAGES=""

    # Bootloader
    BR2_TARGET_UBOOT=y
    BR2_TARGET_UBOOT_BUILD_SYSTEM_KCONFIG=y
    BR2_TARGET_UBOOT_CUSTOM_GIT=y
    BR2_TARGET_UBOOT_CUSTOM_REPO_URL="/home/xiaguangbo/project/rockchip_buildroot/source/u-boot"
    BR2_TARGET_UBOOT_CUSTOM_REPO_VERSION="origin/master"
    BR2_TARGET_UBOOT_BOARD_DEFCONFIG="firefly-rk3399"
    BR2_TARGET_UBOOT_NEEDS_DTC=y
    BR2_TARGET_UBOOT_NEEDS_PYTHON3=y
    BR2_TARGET_UBOOT_NEEDS_ATF_BL31=y
    BR2_TARGET_UBOOT_NEEDS_ATF_BL31_ELF=y
    BR2_TARGET_UBOOT_FORMAT_CUSTOM=y
    BR2_TARGET_UBOOT_FORMAT_CUSTOM_NAME="u-boot.itb"
    BR2_TARGET_UBOOT_SPL=y
    BR2_TARGET_UBOOT_SPL_NAME="idbloader.img"

    # Kernel
    BR2_LINUX_KERNEL=y
    BR2_LINUX_KERNEL_CUSTOM_GIT=y
    BR2_LINUX_KERNEL_CUSTOM_REPO_URL="/home/xiaguangbo/project/rockchip_buildroot/source/linux"
    BR2_LINUX_KERNEL_CUSTOM_REPO_VERSION="origin/master"
    BR2_LINUX_KERNEL_USE_ARCH_DEFAULT_CONFIG=y
    BR2_LINUX_KERNEL_DTS_SUPPORT=y
    BR2_LINUX_KERNEL_INTREE_DTS_NAME="rockchip/rk3399-firefly"
    BR2_LINUX_KERNEL_NEEDS_HOST_OPENSSL=y

    # Filesystem
    BR2_TARGET_GENERIC_HOSTNAME="firefly-rk3399"
    BR2_TARGET_GENERIC_ISSUE="Welcome to firefly-rk3399"
    BR2_TARGET_ROOTFS_EXT2=y
    BR2_TARGET_ROOTFS_EXT2_4=y
    BR2_PACKAGE_HOST_DOSFSTOOLS=y
    BR2_PACKAGE_HOST_GENIMAGE=y
    BR2_PACKAGE_HOST_MTOOLS=y
    BR2_PACKAGE_HOST_UBOOT_TOOLS=y
    BR2_ROOTFS_POST_IMAGE_SCRIPT="support/scripts/genimage.sh"
    BR2_ROOTFS_POST_SCRIPT_ARGS="-c board/firefly/firefly-rk3399/genimage.cfg"
    BR2_ROOTFS_POST_BUILD_SCRIPT="board/firefly/firefly-rk3399/post-build.sh"
    ```

    BR2_TARGET_ARM_TRUSTED_FIRMWARE_PLATFORM：TF-A 对于 rk3399 的适配在 arm-trusted-firmware/plat/rockchip/rk3399  
    BR2_TARGET_UBOOT_BOARD_DEFCONFIG：文件是 u-boot/configs/firefly-rk3399_defconfig  
    BR2_LINUX_KERNEL_INTREE_DTS_NAME：文件是 linux-5.19.1/arch/arm64/boot/dts/rockchip/rk3399-firefly.dts

#### 编译

```
make firefly_rk3399_defconfig
make
```

生成的镜像在 output/images 下

#### 附录

- 删除旧包

    ```
    rm -rf output/target
    ```

- 删除包安装记录

    ```
    find output/ -name ".stamp_target_installed" | xargs rm -rf
    ```