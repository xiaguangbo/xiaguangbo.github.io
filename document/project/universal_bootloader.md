简单、最好的移植性

为了更具有移植性，将某型号的芯片的所有文件都放在一个文件夹里，除了芯片架构、外设驱动等通用文件

首先是 CPU 自带的程序运行并加载一阶段引导程序，一阶段引导程序初始化 CPU 以提供语言运行环境，一阶段引导程序再加载二阶段引导程序，二阶段引导程序初始化外部内存，二阶段引导程序再加载三阶段引导程序，三阶段引导程序就是本体。相当于 bootrom、uboot-spl、uboot